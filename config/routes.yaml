controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

api_login_check:
    path: /api/login_check
    # No controller, intercepted by security firewall json_login

api_token_refresh:
    path: /api/token/refresh
    # No controller, intercepted by security firewall refresh_jwt